export enum QuestionType {
  CHECKBOX = 'checkbox',
  DATE = 'date',
  EMAIL = 'email',
  FILE = 'file',
  NUMBER = 'number',
  PASSWORD = 'password',
  RADIO = 'radio',
  SELECT = 'select',
  TEXT = 'text',
  TEXTAREA = 'textarea',
}
