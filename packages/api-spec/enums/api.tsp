@doc("Defines the API versioning scheme for the LokiForm API. Currently supports version 1.0.")
enum Versions {
  @doc("API version 1.0 - Initial release with core form functionality")
  v1: "1.0",
}
